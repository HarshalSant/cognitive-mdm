# IMPORTANT: Do NOT commit real secrets. Use Sealed Secrets, External Secrets, or a vault.
# This file is a template only — fill in base64-encoded values before applying.
apiVersion: v1
kind: Secret
metadata:
  name: cognitive-mdm-secrets
  namespace: cognitive-mdm
type: Opaque
stringData:
  POSTGRES_USER: "mdm_user"
  POSTGRES_PASSWORD: "REPLACE_ME"
  NEO4J_USERNAME: "neo4j"
  NEO4J_PASSWORD: "REPLACE_ME"
  ANTHROPIC_API_KEY: "REPLACE_ME"
  JWT_SECRET_KEY: "REPLACE_ME_WITH_32_CHAR_RANDOM_STRING"
